import { WindRadiiMaxExtentDataService } from './wind-radii-max-extent.service';

describe('WRM service', () => {
  let wrmService: WindRadiiMaxExtentDataService;

  beforeAll(async () => {});

  afterAll(async () => {});

  it('should create WRM entity', async () => {});
});
